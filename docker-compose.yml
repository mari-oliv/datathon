services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # permite atualizar o modelo sem rebuild
      - ./src/model:/app/src/model:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
